google-adk[extensions]
pdfplumber
duckdb  # local corpus for parsed PDF tables (scripts/pdf_to_duckdb.py + SQL mode)
python-dotenv
boto3  # only needed for LLM_BACKEND=bedrock (LiteLLM uses it for AWS auth)
