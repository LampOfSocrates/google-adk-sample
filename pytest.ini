[pytest]
# auto = async test functions run without needing an explicit decorator
asyncio_mode = auto
markers =
    live: hits the real Gemini API (needs GOOGLE_API_KEY + free-tier quota). Skip with: -m "not live"
