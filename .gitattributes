# Shell scripts must stay LF so the shebang works when the repo is cloned on Linux
# (this repo is edited on Windows, where git would otherwise convert them to CRLF).
*.sh text eol=lf
deploy/docker-entrypoint.sh text eol=lf
